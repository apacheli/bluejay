export const metadata = {
    title: "A Blog",
    description: "This is a blog post.",
    type: "blog",
    date: "2025-09-20",
    tag: "development"
}

# Hello, World! `amogus`

How are you?

```TypeScript
export const hello: boolean = true;

const test: string = "https://google.com/";

// A comment
const hi: number = 5;
const hey = {
    test: true,
};



/**
 * This is an interface.
 */
interface Yeah {
    world?: string;
}
```

```Python
def main():
    """The main program."""
    print("Hello, World!")


if __name__ == "__main__":
    main()
```

```
Generic Codeblock
No Language
```

```TSX
import { transformText } from "./helpers.tsx";

interface MyComponentProps {
    text: string;
}

const MyComponent = (props: MyComponentProps) => {
    return (
        <p>{transformText(props.text)}</p>
    );
};
```

`hello world`

https://developer.mozilla.org/en-US/docs/Web/JavaScript
