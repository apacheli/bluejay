{
    "type": "module",
    "scripts": {
        "lint": "bunx @biomejs/biome lint --write",
        "check": "bunx @biomejs/biome check --write",
        "format": "bunx @biomejs/biome format --write",
        "build": "START_NODE=build bun run main.tsx",
        "serve": "START_MODE=serve bun run main.tsx"
    },
    "dependencies": {
        "bluejay": "https://github.com/apacheli/bluejay"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "@biomejs/biome": "^1.8.3"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    }
}
